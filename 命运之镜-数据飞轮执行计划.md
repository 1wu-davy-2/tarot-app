# 命运之镜 — 数据飞轮执行计划

## Context

当前问题：功能广度足够（17 个后端路由 + 15 个前端页面），但功能之间互相孤立，用户数据不产生复利效应。用户走进来能用所有功能，但没有理由"留下来"。

目标：从"每次用完即走"转变为"用得越久价值越大"。核心策略不是加新功能，而是让现有数据产生沉淀和连接。

## 总体架构

所有阶段独立可部署，按 ROI 排序。每个阶段完成后即可上线验证。

---

## Phase 1: AI 跨解读记忆层 [最高优先级]

**核心改动**：AI 解读时注入用户历史占卜记录，让 AI"记得"你问过什么。

### 后端改动

#### 1a. 新增模型 (`backend/models.py`)
- `ReadingMemory`: 存储关键主题、问题分类、矛盾标记
  - reading_record_id, user_id, key_themes(JSON), question_category, contradiction_with_id

#### 1b. 新增路由 (`backend/routers/memory.py`)
| 端点 | 说明 |
|------|------|
| `GET /api/memory/themes` | 获取已缓存的生命主题总结 |
| `POST /api/memory/themes/generate` | 生成 90 天主题总结 (SSE, 1 quota) |
| `GET /api/memory/contradictions` | 获取矛盾检测结果 |

#### 1c. 修改 `interpret.py`（核心改动，最小投入最大产出）
在 AI 解读前注入近期占卜历史上下文：
```python
# 取最近 10 条阅读记录 → 拼成简洁摘要 → 注入 prompt
# 若 >= 3 条历史，加一致性提示
```
不增加额外 API 调用，只是 prompt 上下文变长 ~500 tokens。

### 前端改动
- **新页面**: `app/insights/page.tsx` — 生命主题面板
- **新组件**: `components/ReadingMemoryPanel.tsx` — 解读后展示历史关联
- **修改**: `lib/api-client.ts` 添加 API 函数
- **修改**: 导航添加 "Insights" 入口

### 投入估算：~3-4 天
### 最快交付（1 天）：只做 Step 1c，立即可用

---

## Phase 2: 日记数据洞察层 [最高优先级]

**核心改动**：把日记数据从"静态记录"变成"可视化洞察"。

### 后端改动

#### 2a. 新模型 (`backend/models.py`)
- `JournalInsightCache`: 缓存计算结果

#### 2b. 新增路由 (`backend/routers/insights.py`)
| 端点 | 说明 |
|------|------|
| `GET /api/insights/card-mood?months=3` | 卡牌-情绪相关性（纯 SQL，无 AI 成本） |
| `GET /api/insights/time-patterns?months=3` | 时间模式：周几/月相的情绪趋势（纯 SQL） |
| `POST /api/insights/annual-report` | 年度灵魂报告 (SSE, 1 quota) |

### 前端改动
- **新页面**: `app/journal/insights/page.tsx` — 三个 tab：卡牌-情绪/时间模式/年度报告
- **新组件**: `components/JournalInsightsChart.tsx` — 用 recharts（已安装）做柱状图/折线图
- **修改**: `app/journal/page.tsx` 加迷你洞察卡片 + insights 入口

### 投入估算：~2-3 天
### 最快交付（1 天）：Step 2b + 2c，纯 SQL 无 AI 成本

---

## Phase 3: 占星个性化 [高优先]

**核心改动**：用户填了出生信息 → 运势真正个性化。

### 后端改动

#### 3a. 新依赖
```
flatlib==0.2.3  # 纯 Python 天文计算，不需要额外数据文件
```

#### 3b. 新工具模块 (`backend/lib/astrology.py`)
- `get_current_transits_summary(user)` → 返回中文行进摘要
- `is_retrograde_affecting(user, planet)` → 检查逆行是否影响该用户
- `get_critical_months(user, 6)` → 未来 6 个月的关键月份

#### 3c. 修改 `fortune.py` 和 `interpret.py`
- 运势生成和解读时注入个人星象行进上下文
- 新增 `GET /api/fortune/personal-transits`

### 前端改动
- **新组件**: `components/PersonalTransitCard.tsx`
- **修改**: `app/fortune/page.tsx` 添加"个人模式"开关
- **修改**: `app/profile/page.tsx` 展示当前行进

### 投入估算：~2-3 天
### 核心难点：astrology.py 的工具函数编写

---

## Phase 4: 学习→实践闭环 [高优先]

**核心改动**：学习和占卜不再孤立。

### 后端改动

#### 4a. 新模型
- `QuizHistory`: 记录错题（module_id, card_id, is_correct）
- `PracticeReading`: 课后练习占卜

#### 4b. 新路由 (`backend/routers/learning.py`)
| 端点 | 说明 |
|------|------|
| `POST /api/learning/quiz-result` | 记录答题结果 |
| `GET /api/learning/wrong-cards` | 获取常错卡牌列表 |
| `GET /api/learning/depth-tier` | 返回学习深度等级（novice/student/adept/sage） |
| `POST /api/learning/practice-reading` | 创建课后练习占卜 |

#### 4c. 修改 `interpret.py`
- 根据 learning depth tier 调整解读深度
- 抽到"错题卡"时添加复习提示

### 前端改动
- **修改**: `components/LessonView.tsx` 课后显示"练习占卜"按钮
- **修改**: `app/spread/page.tsx` 错题卡显示复习标记
- **修改**: `components/QuizModal.tsx` 记录错题到后端

### 投入估算：~2-3 天

---

## Phase 5: 卡牌收集游戏化 [中优先]

**核心改动**：每次抽牌都可能解锁新牌，78 张收集驱动。

### 后端
- 新模型 `CardCollection` (user_id, card_id, first_seen_at, seen_count)
- `POST /api/collection/track` — 记录抽到的牌
- `GET /api/collection/status` — 收集进度

### 前端
- **新页面**: `app/collection/page.tsx` — 78 张牌网格，已收集的亮色，未收集的灰色
- **新组件**: `components/CollectionUnlockToast.tsx` — 解锁弹窗
- **修改**: `app/spread/page.tsx` 抽牌后检查解锁

### 投入估算：~2 天，主要是前端 UI

---

## Phase 6: 社区互动化 [中优先]

### 后端
- 新模型: `SharedReading`, `SharedReadingLike`, `SharedReadingComment`
- `POST /api/community/share` / `GET /api/community/feed` / like / comment

### 前端
- **新页面**: `app/community/page.tsx` — 社区分享流
- **新组件**: `components/ReadingShareCard.tsx` — 解读分享卡
- **修改**: `components/ShareButton.tsx` 添加"分享到社区"

### 投入估算：~3 天

---

## Phase 7: 梦境深化 [低优先]

### 后端
- 修改 `dream.py`: 检测重复符号（同一 tag 出现 3+ 次时提示）
- 梦境→塔罗牌推荐（一个轻量 AI 调用）
- `GET /api/fortune/dreams/recurring-symbols`

### 前端
- 修改 `DreamModal.tsx`: 重复符号警告 + 推荐卡牌 + "抽这张牌"快捷入口
- 修改 `app/dreams/page.tsx`: 重复符号区块

### 投入估算：~1-2 天

---

## 最快验证路径（4 天 MVP）

如果时间有限，只做这些就能让产品感觉"活"起来：

| 天 | 做什么 | 效果 |
|----|--------|------|
| 1 | Phase 1 Step 1c: interpret.py 注入历史上下文 | AI"记住"你了 |
| 2 | Phase 2 Step 2b+2c: 卡牌-情绪 + 时间模式 | 日记数据有洞察 |
| 3 | Phase 3 Step 3c: fortune 注入个人星象 | 运势真正个人化 |
| 4 | Phase 4 Step 4e: interpret 根据学习等级调整 | 学得越多越深度 |

---

## 关键文件清单

- `backend/routers/interpret.py` — Phase 1, 3, 4 的核心修改点
- `backend/models.py` — 所有阶段新增模型
- `backend/main.py` — 注册路由 + ensure_columns
- `lib/api-client.ts` — 所有阶段新增 API 封装
- `app/spread/page.tsx` — Phase 1, 4, 5 的抽牌流程修改
- `lib/tarot-lessons.ts` — Phase 4 学习系统扩展
- `lib/achievements.ts` — Phase 1, 5 成就扩展

## 可复用的现有模式

- SSE 流: 参考 `journal.py` monthly_report 或 `fortune.py` _stream_live_ai
- 数据库表创建: `main.py` 的 `ensure_columns()` 模式，不需要单独 migration
- 配额消耗: 复用 `checkin.py` 的 `get_or_create_quota()`
- 缓存模式: FortuneCache / CompatibilityCache 的 upsert 模式
- 调度器: `fortune.py` 的 `start_fortune_scheduler()` 模式

## 唯一新依赖

- Phase 3: `flatlib==0.2.3` (纯 Python，不需要额外数据文件)
- 其余阶段全部使用现有技术栈
